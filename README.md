# TripleDStore
TripleDStore is an e-commerce web application built using python(django), HTML, and Bootstrap. The application comes with the default django administration site and with payment integrated. The payment system is powered by ...  

# Install the project to your local machine.
- Create and active a python virtual environment of your choice and install the project dependencies frin requirements.txt file.
- The project uses SQLite so no need to migrate your database. Simply run the server using "python manage.py runserver"
